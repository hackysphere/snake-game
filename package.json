{
  "name": "snake-game-firebase",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",

    "serve-dev": "NODE_ENV=development node backend/server.js",
    "serve": "NODE_ENV=production node backend/server.js"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "express": "^5.1.0",
    "firebase": "^12.1.0"
  }
}
