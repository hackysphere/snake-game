{
  "name": "snake-game",
  "version": "0.1.3",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve-dev": "NODE_ENV=development node --watch backend/server.js",
    "serve": "NODE_ENV=production node --env-file=.env backend/server.js",
    "serve-noenvfile": "NODE_ENV=production node backend/server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "vite": "^7.3.0"
  }
}
