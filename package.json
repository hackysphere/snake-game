{
  "name": "snake-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve-dev": "NODE_ENV=development node --watch backend/server.js",
    "serve": "NODE_ENV=production node backend/server.js"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
